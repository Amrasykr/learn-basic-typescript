describe("Data Type", () =>{
    it('must be declare', () => {
        const name: string = "ammar";
        const balance:number = 100000
        const isVip:boolean = true

        console.info(name)
        console.info(balance)
        console.info(isVip)
    });
})