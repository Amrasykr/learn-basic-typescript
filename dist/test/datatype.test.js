"use strict";
describe("Data Type", () => {
    it('must be declare', () => {
        const name = "ammar";
        const balance = 100000;
        const isVip = true;
        console.info(name);
        console.info(balance);
        console.info(isVip);
    });
});
